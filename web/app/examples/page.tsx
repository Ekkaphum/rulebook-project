import { ExamplesPageView } from "@/components/views/PageViews";
export const metadata = { title: "Rulebooks studied", description: "35 rulebooks named as exemplary and 33 named as problematic — with the specific reason and source for each." };
export default function Page() { return <ExamplesPageView locale="en" />; }
