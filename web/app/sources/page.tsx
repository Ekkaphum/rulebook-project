import { SourcesPageView } from "@/components/views/PageViews";
export const metadata = { title: "Sources", description: "All 106 primary sources harvested for this project, with clickable URLs." };
export default function Page() { return <SourcesPageView locale="en" />; }
